///Solicitar al usuario que ingrese la base y altura de un rectángulo, y calcular y mostrar por 
///pantalla el área y perímetro del mismo
///area = base * altura
///perimetro = 2 * altura + 2 * base

Algoritmo BaseYAltura
	Definir area, base, altura, perimetro Como Real
	
	Escribir "Ingrese el valor de la base: ";
	Leer base
	Escribir "Ingrese el valor de la altura: ";
	Leer altura
	
	//PROCESO
	area = base * altura;
	Escribir "El resultado del área del rectángulo es igual a: " (area);
	
	perimetro = (2*altura) + (2*base); 
	Escribir "El resultado del perímetro del rectángulo es igual a: " (perimetro)
	
FinAlgoritmo
